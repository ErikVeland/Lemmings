# 1.5 test build 48

- Press H for level hints. I and F1 remain available.
- Use the toolbar buttons to rewind two seconds or step backward and forward by one tick.
- Open hints from the toolbar in Classic, Lemmings 2 and Lemmings 3.
- Keep play paused after stepping or rewinding. Unavailable rewind controls are disabled.

Local Apple silicon Game Center snapshot, development signed for registered Macs. Not notarised.

Validation: direct toolbar clicks and history restoration in all three engines; Classic flat and CRT source panels; sequel start and paused renders; hint input in both control modes. Classic rewind remains unavailable for rulesets that do not support it. Full campaign playthroughs were not repeated for this UI change.
