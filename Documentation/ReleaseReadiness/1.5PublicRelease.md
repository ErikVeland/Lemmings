# macOS 1.5 public release readiness

Date: 25 September 2026

Candidate: `codex/1.5`, build 45

Source revision: `0b0b8fd5e85d617cd68b48308bc8e585306a11c4`

## Release scope

The release contains the completed 352-level Classic campaign. Lemmings 2 and
Lemmings 3 remain Preview. NeoLemmix remains Beta or Preview. The candidate
fixes the launch, input, startup, music-transition and gameplay-cursor issues
recorded since the 1.2 build 41 public release.

## Current gate status

| Gate | Status | Evidence or closure condition |
| --- | --- | --- |
| 1.5 source and NeoLemmix checks | Passed | `Scripts/check-1.5-neolemmix.sh` and its end-to-end source checks pass. |
| Cursor geometry and system-pointer policy | Passed | Pointer capture and cursor geometry tests pass. The playfield visual test needs the local `Content/lemming1.pc` fixture. |
| Controller and speed input | Passed | Controller binding and gameplay-speed suites pass. |
| Native sequel data | Passed | `Scripts/run-sequel-data-tests.sh` passes after the compiler-safe test expression fix. |
| Crash-free packaged launch | Open | Build a fresh app with bundled data, then run the empty-home launch smoke test. |
| Full app and Classic route audit | Open | The local app bundle has no `Ports` data and the checkout has no `Content/lemming1.pc`. |
| Rescue proof catalogue | Open | The current engine fingerprint is `799fdf9f5b4720802d6eff75a98023b9f4229346fdfce13a6f3d83399dc45c6d`. The committed catalogue records an older fingerprint. Re-run the audit with the authorised content set. |
| HDR and hardware performance | Open | Metal is unavailable on this host. Run the release performance matrix on the release Mac. |
| Developer ID, Monterey and Gatekeeper | Open | Build from the frozen commit with the release Mac credentials and Monterey worktree. |
| Live Sparkle update | Open | Generate the signed 1.5 appcast, publish the archive, then update from the previous public build. |
| NeoLemmix strict and reference replay gates | Open by scope | The pinned corpus still contains unsupported mechanics and no reference `.nxrp` corpus. Do not advertise full compatibility. |

## Required release handoff

1. Freeze the source after optimisation work stops.
2. Supply the authorised commercial content and build a fresh app bundle.
3. Re-run the audit and Trolley verification against that bundle.
4. Run the standard, Monterey and Game Center package checks.
5. Sign, notarise and Gatekeeper-check the two Developer ID archives.
6. Generate the signed appcast and test update installation from the public 1.2 build.
7. Publish only after every release-scope gate records the same source revision.
