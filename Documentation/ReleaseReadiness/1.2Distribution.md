# 1.2 public distribution verification

Verified 25 September 2026. This checks distribution of existing build 39;
it does not establish new gameplay, hardware or accessibility coverage.

- Release source: `2149c2309771182da14624f31c2ac63e693fe1d3`.
- Archive: `UltimateLemmings-1.2-build39.zip` (428869830 bytes).
- SHA-256: `5b06fc2329323f772b20e972c3d34e177aaf3c71ec8a8290cba5df7730a2db39`.
- App version: 1.2, build 39. Minimum macOS: 12.3 in the plist and both executable slices.
- Architectures: arm64 and x86_64.
- Developer ID: Erik Veland, team `54WU29TRTY`.
- Deep, strict code signature verification: passed, including Sparkle and its helpers.
- Stapled notarisation ticket validation: passed.
- Gatekeeper with Safari quarantine applied: accepted, `Notarized Developer ID`.
- Apple submission: `4c431801-993c-436d-bafb-2ffcfbe21c28`, Accepted, no issues.
- All 30 ticket entries matched the shipped code hashes for their architectures,
  including Sparkle.framework, Updater.app, Autoupdate, Downloader.xpc and Installer.xpc.
- Ed25519 archive signature: verified with CryptoKit against `SUPublicEDKey`
  read from the extracted shipping app, without reading a private key.
- `Scripts/check-1.2-release-inputs.sh`: passed.

The archive is unchanged from the RC3 download. The public v1.2.0 release and
appcast use this same signed archive. Build 39 users already have these binaries.
Release notes retain sequel preview status, the known artwork and controller
issues, and the physical Intel/minimum-macOS validation gap. The public package
has no Game Center device-registration requirement.

## Publication

- GitHub reports `v1.2.0` as latest, with draft and prerelease both false.
- An unauthenticated download through `/releases/latest/download/` completed
  and matched the SHA-256 above.
- GitHub's main-branch contents API matched the published local appcast blob.
  The raw-file CDN initially retained the previous feed under its five-minute
  cache policy. That feed also serves the same verified build 39 archive.
